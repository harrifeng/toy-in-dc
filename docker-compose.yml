version: '2'
services:
  db:
    image: mysql:5.6
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - ./toy_app:/myapp
    ports:
      - "3003:3000"
    depends_on:
      - db
